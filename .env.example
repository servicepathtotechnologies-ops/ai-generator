PORT=3002

# LLM
GEMINI_API_KEY=

# Auth (shared with worker for token verification)
AWS_REGION=
COGNITO_USER_POOL_ID=
COGNITO_ISSUER=

# Worker (for node catalog fetch + service auth)
WORKER_URL=http://localhost:3001
AI_GENERATOR_SERVICE_KEY=

# Redis (shared cache)
REDIS_URL=
